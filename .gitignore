# Dependencies
/node_modules
/.pnp
.pnp.js

# Build outputs
/dist
/build
/out
/.next
/.nuxt
/coverage

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# IDEs and editors
.idea/
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# TypeScript
*.tsbuildinfo
# Might be generated by tsc if `outDir` is not specified or same as source
# and if you are compiling .ts to .js directly in source folders.
# Adjust if your tsconfig.json `outDir` is different.
# index.js (if compiled from index.tsx and you don't want to commit the JS)
# *.js (if you only want to commit .ts files)
# *.js.map (if you generate source maps and don't want to commit them)

# Optional: Add if you are using specific tools
# e.g. Next.js
# /.next/
# /out/

# e.g. Parcel
# /.cache/
# /dist/

# e.g. Webpack
# /public/build/
# /public/bundles/

# Test runner files
/coverage
/.nyc_output

# Temporary files
*.tmp
*.swp
*.swo

# Project specific files (if any, e.g. secrets not covered by .env)
# secrets.json
# config.local.js